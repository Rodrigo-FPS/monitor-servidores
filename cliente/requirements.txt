# Dependencias del agente de heartbeat con versiones fijadas.
# Para maxima integridad puede generarse un lock con hashes e instalarse con:
#   pip install --require-hashes -r requirements.txt
httpx==0.27.2
cryptography==43.0.1
